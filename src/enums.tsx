export enum Msgs {
  LOGGED_IN = "you logged in successfully",
}

export enum Errs {
  ERR = "Error",
  CALL_NOT_SUCCESS = "Call was not successful",
}

export enum Words {
  TOKEN = "Token",
  LOGIN = "Login",
}
